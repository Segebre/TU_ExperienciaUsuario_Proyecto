<tr>
  <th><%= link_to campaign.title, campaign_path(campaign) %></th>
  <th><%= campaign.review %></th>
  <th><%= campaign.goal %></th>
  <th>Amount donated</th>
  <th><%= campaign.created_at %></th>
  <th><%= campaign.user.name %></th>
</tr>